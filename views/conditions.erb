<div class="container">
    <div class="row">
        <div class="col-md-8">
            <% @conditions.each do |c| %>
                <h3><%= c.name %></h3>
                <% c.description.each do |d| %>
                    <p><%= d %></p>
                <%end%>
            <%end%>
        </div>
    </div>
</div>
